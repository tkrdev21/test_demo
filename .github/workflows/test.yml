name: Shell Commands
  on: [push]
jobs:
  run-shell-command:
    runs-on: ubuntu-latest
    steps:
    - name: echo string
      run: echo "welcome to the world"
    - name: multiline script
      run: |
        node -v
        npm -v
        cat /etc/os-release